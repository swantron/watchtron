{
  "name": "@watchtron/control-plane",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "watchtron control plane: OTLP/HTTP JSON receiver, ephemeral span buffer, verify API, status dashboard.",
  "license": "MIT",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test test"
  },
  "dependencies": {
    "@watchtron/registry": "*",
    "express": "^5.0.0"
  }
}
